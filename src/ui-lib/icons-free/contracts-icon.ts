import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { ContractsIcon as ContractsIconH } from '@hugeicons/core-free-icons';

export const ContractsIcon = createIconComponent("ContractsIcon", {
  "stroke.rounded": ContractsIconH,
} as Record<string, any>, "stroke");
