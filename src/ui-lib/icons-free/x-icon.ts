import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { NewTwitterIcon } from '@hugeicons/core-free-icons';

export const XIcon = createIconComponent("NewTwitterIcon", {
  "stroke.rounded": NewTwitterIcon,
} as Record<string, any>, "stroke");
