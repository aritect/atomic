import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { ChartColumnIcon as ChartColumnIconH } from '@hugeicons/core-free-icons';

export const ChartColumnIcon = createIconComponent("ChartColumnIcon", {
  "stroke.rounded": ChartColumnIconH,
} as Record<string, any>, "stroke");
