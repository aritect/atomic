import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { SmileIcon as SmileIconH } from '@hugeicons/core-free-icons';

export const SmileIcon = createIconComponent("SmileIcon", {
  "stroke.rounded": SmileIconH,
} as Record<string, any>);
