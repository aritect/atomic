import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { FilterResetIcon as FilterResetIconH } from '@hugeicons/core-free-icons';

export const FilterResetIcon = createIconComponent("FilterResetIcon", {
  "stroke.rounded": FilterResetIconH,
} as Record<string, any>);
