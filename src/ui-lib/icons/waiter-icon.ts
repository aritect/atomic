import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { WaiterIcon as WaiterIconH } from '@hugeicons/core-free-icons';

export const WaiterIcon = createIconComponent("WaiterIcon", {
  "stroke.rounded": WaiterIconH,
} as Record<string, any>);
