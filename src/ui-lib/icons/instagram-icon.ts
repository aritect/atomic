import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { InstagramIcon as InstagramIconH } from '@hugeicons/core-free-icons';

export const InstagramIcon = createIconComponent("InstagramIcon", {
  "stroke.rounded": InstagramIconH,
} as Record<string, any>);
