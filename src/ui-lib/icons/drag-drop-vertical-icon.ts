import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { DragDropVerticalIcon as DragDropVerticalIconH } from '@hugeicons/core-free-icons';

export const DragDropVerticalIcon = createIconComponent("DragDropVerticalIcon", {
  "stroke.rounded": DragDropVerticalIconH,
} as Record<string, any>);
