import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { ArrowRightDoubleIcon } from '@hugeicons/core-free-icons';

export const ChevronsRightIcon = createIconComponent("ArrowRightDoubleIcon", {
  "stroke.rounded": ArrowRightDoubleIcon,
} as Record<string, any>);
