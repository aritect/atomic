import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { GridViewIcon as GridViewIconH } from '@hugeicons/core-free-icons';

export const GridViewIcon = createIconComponent("GridViewIcon", {
  "stroke.rounded": GridViewIconH,
} as Record<string, any>);
