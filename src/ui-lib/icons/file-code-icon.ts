import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { DocumentCodeIcon } from '@hugeicons/core-free-icons';

export const FileCodeIcon = createIconComponent("DocumentCodeIcon", {
  "stroke.rounded": DocumentCodeIcon,
} as Record<string, any>);
