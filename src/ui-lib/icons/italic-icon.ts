import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { TextItalicIcon } from '@hugeicons/core-free-icons';

export const ItalicIcon = createIconComponent("TextItalicIcon", {
  "stroke.rounded": TextItalicIcon,
} as Record<string, any>);
