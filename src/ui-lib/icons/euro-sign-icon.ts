import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { EuroIcon } from '@hugeicons/core-free-icons';

export const EuroSignIcon = createIconComponent("EuroIcon", {
  "stroke.rounded": EuroIcon,
} as Record<string, any>);
