import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { BinaryCodeIcon } from '@hugeicons/core-free-icons';

export const BinaryIcon = createIconComponent("BinaryCodeIcon", {
  "stroke.rounded": BinaryCodeIcon,
} as Record<string, any>);
