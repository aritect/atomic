import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { LinkCircleIcon as LinkCircleIconH } from '@hugeicons/core-free-icons';

export const LinkCircleIcon = createIconComponent("LinkCircleIcon", {
  "stroke.rounded": LinkCircleIconH,
} as Record<string, any>);
