import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { CarrotIcon as CarrotIconH } from '@hugeicons/core-free-icons';

export const CarrotIcon = createIconComponent("CarrotIcon", {
  "stroke.rounded": CarrotIconH,
} as Record<string, any>);
