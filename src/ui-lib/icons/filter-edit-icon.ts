import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { FilterEditIcon as FilterEditIconH } from '@hugeicons/core-free-icons';

export const FilterEditIcon = createIconComponent("FilterEditIcon", {
  "stroke.rounded": FilterEditIconH,
} as Record<string, any>);
