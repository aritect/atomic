import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { FireSecurityIcon as FireSecurityIconH } from '@hugeicons/core-free-icons';

export const FireSecurityIcon = createIconComponent("FireSecurityIcon", {
  "stroke.rounded": FireSecurityIconH,
} as Record<string, any>);
