import { createIconComponent } from "@/utils-ui-lib/create-icon-component";
import { UnfoldLessIcon as UnfoldLessIconH } from '@hugeicons/core-free-icons';

export const UnfoldLessIcon = createIconComponent("UnfoldLessIcon", {
  "stroke.rounded": UnfoldLessIconH,
} as Record<string, any>);
